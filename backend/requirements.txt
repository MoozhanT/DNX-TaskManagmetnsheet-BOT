fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2

# احراز هویت پنل وب: هش پسورد و JWT
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# بات تلگرام
aiogram==3.13.1
# فقط وقتی TELEGRAM_PROXY_URL ست شده باشد استفاده می‌شود (عبور از فیلترینگ با SOCKS5)
aiohttp-socks==0.9.1

# scheduler یادآوری
apscheduler==3.10.4
