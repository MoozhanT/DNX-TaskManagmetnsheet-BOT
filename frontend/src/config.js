// آدرس بک‌اند از فایل .env خوانده می‌شود (متغیر VITE_API_BASE_URL)؛ اگر نبود، آدرس پیش‌فرض توسعه استفاده می‌شود.
// توجه: از ?? استفاده شده نه ||، چون در Docker مقدار خالی "" عمداً یعنی
// «همان آدرس فرانت را با مسیر نسبی صدا بزن» (پشت nginx) و نباید با مقدار پیش‌فرض جایگزین شود.
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL ?? 'http://127.0.0.1:8000';
